# Portfolio — public demo writeup

The last mile of a portfolio project is the story. `DEMO.md` is what someone opens before they clone.

## Tasks

1. Write `/workspace/DEMO.md` with:
   - **Architecture** (mention Go, Docker, Kubernetes)
   - **Local run**
   - **Kubernetes demo** (mention Ingress or `payments.local`)
   - **What I learned**
2. Write `/workspace/docs/ARCHITECTURE.md` including a `mermaid` code fence
